import React from 'react';

import Repolink from '@/components/repolink/Repolink';

function RepoLinkPage() {
  return (
    <div className={'mx-auto max-w-[380px]'}>
      <Repolink />
    </div>
  );
}

export default RepoLinkPage;
